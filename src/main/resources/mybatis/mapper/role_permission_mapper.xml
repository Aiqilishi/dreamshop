<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.mmko.dao.IRolePermissionDao">

    <resultMap id="DataMap" type="cn.mmko.po.RolePermissionPo">
        <id property="id" column="id"/>
        <result property="roleId" column="role_id"/>
        <result property="permissionId" column="permission_id"/>
    </resultMap>
    <select id="queryPermissionByRoleIds" resultType="java.lang.Long" parameterType="long">
         select  permission_id
         from t_role_permission where role_id in
         <foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
            #{item}
        </foreach>

    </select>


</mapper>