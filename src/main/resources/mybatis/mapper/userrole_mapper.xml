<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.mmko.dao.IUserRoleDao">

    <resultMap id="DataMap" type="cn.mmko.po.UserRolePo">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
    </resultMap>

    <select id="queryUserRoleByUserId" resultType="java.lang.Long" parameterType="long">
        select role_id from t_user_role where user_id=#{userId}
    </select>
    <insert id="insertUserRole" parameterType="cn.mmko.po.UserRolePo">
        insert into t_user_role(user_id,role_id) values(#{userId},#{roleId})

    </insert>

</mapper>